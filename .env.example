# ==========================================================
# BLITZ LEIHEN — Variables d'environnement
# Copier ce fichier en .env et remplir les valeurs réelles.
# NE JAMAIS commiter le fichier .env dans Git.
# ==========================================================

# ----------------------------------------------------------
# SERVEUR
# ----------------------------------------------------------
PORT=3000
NODE_ENV=development

# URL du frontend (pour CORS) — séparer plusieurs URLs par des virgules
# ⚠️ Sur Render, cette variable se règle dans l'onglet Environment du
# service backend (blitzback), PAS dans un fichier .env sur le serveur.
FRONTEND_URL=http://localhost:8080,https://blitzw.onrender.com

# ----------------------------------------------------------
# MONGODB ATLAS
# Obtenir sur : https://cloud.mongodb.com
# Format : mongodb+srv://USER:PASSWORD@cluster.mongodb.net/DBNAME
# ----------------------------------------------------------
MONGODB_URI=mongodb+srv://USERNAME:PASSWORD@cluster0.xxxxx.mongodb.net/blitz-leihen?retryWrites=true&w=majority

# ----------------------------------------------------------
# JWT (Authentification administrateur)
# Générer un secret fort : node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# ----------------------------------------------------------
JWT_SECRET=REMPLACER_PAR_UN_SECRET_FORT_64_CARACTERES_MINIMUM
JWT_EXPIRES_IN=8h

# ----------------------------------------------------------
# EMAIL
# ⚠️ Sur Render, le plan gratuit bloque le SMTP sortant (ports 25/465/587).
# Option recommandée (gratuite, fonctionne sur Render free) :
#   BREVO_API_KEY → envoi via l'API HTTP de Brevo (port 443, jamais bloqué)
#   Récupérer la clé sur : https://app.brevo.com/settings/keys/api
# Si BREVO_API_KEY n'est pas défini, le code retombe sur le SMTP classique
# ci-dessous (utile en local, ou si le service Render est sur un plan payant).
# ----------------------------------------------------------
BREVO_API_KEY=

# Option 1 : Gmail (activer "App passwords" dans Google Account)
# Option 2 : SMTP professionnel (Brevo, Mailgun, SendGrid, OVH...)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=votre.email@gmail.com
SMTP_PASS=votre_app_password_gmail

# Adresse d'expédition (affiché dans les emails)
EMAIL_FROM_NAME=Blitz Leihen
EMAIL_FROM_ADDRESS=noreply@blitz-leihen.de

# Adresse du conseiller qui reçoit les notifications de nouvelles demandes
CONSEILLER_EMAIL=conseiller@blitz-leihen.de

# ----------------------------------------------------------
# SMS — Architecture préparée pour Twilio
# Obtenir sur : https://www.twilio.com
# Laisser vide pour désactiver les SMS
# ----------------------------------------------------------
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=
SMS_ENABLED=false

# ----------------------------------------------------------
# SÉCURITÉ
# ----------------------------------------------------------
# Nombre maximum de requêtes par IP par fenêtre de temps
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW_MS=900000

# Clé de chiffrement pour les données sensibles (optionnel)
ENCRYPTION_KEY=

# ----------------------------------------------------------
# ADMIN PAR DÉFAUT
# ⚠️ ADMIN_EMAIL a un double rôle :
#   1. Compte créé au premier démarrage (changer le mot de passe après)
#   2. Reçoit AUSSI en copie chaque notification de nouvelle demande /
#      abandon de formulaire, en plus de CONSEILLER_EMAIL (si différent)
# ----------------------------------------------------------
ADMIN_EMAIL=admin@blitz-leihen.de
ADMIN_PASSWORD=BlitzAdmin2025!
ADMIN_NAME=Administrateur
